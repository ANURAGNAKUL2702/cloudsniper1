name: CI

on:
  push:
    branches: 
      - main

jobs:
  fail-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Show environment
        run: echo "Running CI on GitHub Actions"

      - name: Force failure (for webhook testing)
        run: exit 1
